rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 이미지 파일: 누구나 읽기 가능, 관리자(로그인 사용자)만 쓰기 가능
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
